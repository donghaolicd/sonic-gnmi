#!/usr/bin/env python3
"""Thin entrypoint for the ado-sandbox developer tool.

Resolves and (in later epics) executes the canonical ADO pipeline YAML locally.
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from ado_sandbox import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
